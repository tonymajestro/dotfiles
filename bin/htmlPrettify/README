# htmlpretty

Pretty print html read from standard input. Useful for formatting html inside text editors such as Vim/Neovim.

## Usage

```bash
python3 -m venv venv

source venv/bin/activate
python3 htmlpretty.py

# Install to /usr/local/bin
sudo just install
htmlpretty
```
